import React from  'react';
//import Button from './Button';

//Создание компонента внутри файла JS через стрелочную функцию (более профессионально)
const Header  = () => {
    return( <header>ReactLogo</header>)
   }

export default Header   